Linux:

# Python 3

Install Python 3 in your system

# Virtual ENV

You will need virtualenv. As root:

$ pip install virtualenv

# Install

run before_first_run.sh

Note: This process will fail without python3 in system

# Running

1. Run run.sh for webserver
2. Open your browser
3. Open localhost:5000

